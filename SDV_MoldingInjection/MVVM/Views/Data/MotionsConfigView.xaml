<UserControl x:Class="SDV_MoldingInjection.MVVM.Views.MotionsConfigView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:SDV_MoldingInjection.MVVM.ViewModels"
             xmlns:ui="clr-namespace:EQX.UI.Controls;assembly=EQX.UI"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type={x:Type vm:MotionsConfigViewModel}}"
             d:DesignHeight="450"
             d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <ui:MotionsSpeedConfigView MotionList="{Binding DataContext.MotionList, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                   IsEnabled="{Binding DataContext.MachineStatus.IsStandByProcessMode, RelativeSource={RelativeSource AncestorType=UserControl}}" />

        <ui:ImageButton Grid.Row="1"
                        Margin="5"
                        Width="200"
                        Height="50"
                        FontSize="16"
                        Content="{DynamicResource str_Save}"
                        Command="{Binding SaveMotionConfigCommand}"
                        Image="{StaticResource image_save_all_selected}" />
    </Grid>
</UserControl>
