<UserControl
    x:Class="SDV_MoldingInjection.MVVM.Views.NavigateMenuView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:SDV_MoldingInjection.MVVM.Views"
    mc:Ignorable="d"
    xmlns:vm="clr-namespace:SDV_MoldingInjection.MVVM.ViewModels"
    xmlns:ui="clr-namespace:EQX.UI.Controls;assembly=EQX.UI"
    d:DataContext="{d:DesignInstance Type=vm:NavigateMenuViewModel}"
    d:DesignHeight="70"
    d:DesignWidth="800">

    <UserControl.Resources>
        <Style
            TargetType="{x:Type ui:ImageRadioButton}"
            BasedOn="{StaticResource {x:Type ui:ImageRadioButton}}">
            <Style.Setters>
                <Setter
                    Property="Padding"
                    Value="5 15" />
                <Setter
                    Property="Margin"
                    Value="3 0" />
                <Setter
                    Property="Width"
                    Value="95" />
                <Setter
                    Property="FontSize"
                    Value="11" />
            </Style.Setters>
        </Style>
    </UserControl.Resources>

    <Grid
        Background="{StaticResource SecondaryBackgroundColor}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition
                Width="Auto" />
            <ColumnDefinition
                Width="*" />
            <ColumnDefinition
                Width="Auto" />
        </Grid.ColumnDefinitions>

        <ItemsControl
            ItemsSource="{Binding LeftNavigationButtons}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <ui:ImageRadioButton
                        GroupName="VMNavigateButton"
                        Width="105"
                        Content="{Binding Label}"
                        Command="{Binding DataContext.NavigationButtonClickCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                        CommandParameter="{Binding .}"
                        DisabledImage="{Binding DisabledImageKey, Converter={StaticResource ImageKeyToImageConverter}}"
                        Image="{Binding ImageKey, Converter={StaticResource ImageKeyToImageConverter}}">
                    </ui:ImageRadioButton>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel
                        Orientation="Horizontal"
                        DockPanel.Dock="Left" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
        </ItemsControl>

        <StackPanel
            Orientation="Horizontal"
            DockPanel.Dock="Right"
            Grid.Column="2">
            <ItemsControl
                ItemsSource="{Binding RightNavigationButtons}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <ui:ImageRadioButton
                            GroupName="VMNavigateButton"
                            Width="107"
                            Content="{Binding Label}"
                            Command="{Binding DataContext.NavigationButtonClickCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                            CommandParameter="{Binding .}"
                            DisabledImage="{Binding DisabledImageKey, Converter={StaticResource ImageKeyToImageConverter}}"
                            Image="{Binding ImageKey, Converter={StaticResource ImageKeyToImageConverter}}">
                        </ui:ImageRadioButton>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel
                            Orientation="Horizontal"
                            DockPanel.Dock="Left" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
            <ui:ImageButton
                Content="Exit"
                Image="{StaticResource image_exit_normal_dark}"
                Command="{Binding ExitCommand}"
                FontSize="11"
                Width="105"
                Margin="3 0 5 0" />
        </StackPanel>
    </Grid>
</UserControl>
