<UserControl x:Class="SDV_MoldingInjection.MVVM.Views.EBoxMainPowerMonitoringView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SDV_MoldingInjection.MVVM.Views"
             xmlns:vm="clr-namespace:SDV_MoldingInjection.MVVM.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type={x:Type vm:EBoxMainPowerMonitoringViewModel}}"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style TargetType="{x:Type TextBox}">
            <Style.Setters>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type TextBox}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"
                                                      SharedSizeGroup="TextBoxDescriptionWidth" />
                                    <ColumnDefinition Width="3*" />
                                </Grid.ColumnDefinitions>
                                <Border CornerRadius="5"
                                        Grid.Column="1"
                                        BorderThickness="1">
                                    <Border.Style>
                                        <Style TargetType="{x:Type Border}">
                                            <Style.Triggers>
                                                <MultiDataTrigger>
                                                    <MultiDataTrigger.Conditions>
                                                        <Condition Binding="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=IsFocused}"
                                                                   Value="True" />
                                                        <Condition Binding="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=IsReadOnly}"
                                                                   Value="False" />
                                                    </MultiDataTrigger.Conditions>
                                                    <MultiDataTrigger.Setters>
                                                        <Setter Property="BorderBrush"
                                                                Value="#0366d6" />

                                                        <Setter Property="Background"
                                                                Value="White" />
                                                    </MultiDataTrigger.Setters>
                                                </MultiDataTrigger>
                                                <DataTrigger Binding="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=IsFocused}"
                                                             Value="True">
                                                    <DataTrigger.Setters>
                                                        <Setter Property="Effect">
                                                            <Setter.Value>
                                                                <DropShadowEffect ShadowDepth="0"
                                                                                  BlurRadius="6"
                                                                                  Opacity="0.5" />
                                                            </Setter.Value>
                                                        </Setter>

                                                    </DataTrigger.Setters>
                                                </DataTrigger>
                                            </Style.Triggers>

                                            <Style.Setters>
                                                <Setter Property="BorderBrush"
                                                        Value="Silver" />
                                                <Setter Property="Background"
                                                        Value="WhiteSmoke" />

                                            </Style.Setters>
                                        </Style>
                                    </Border.Style>
                                </Border>
                                <Label Grid.Column="0"
                                       VerticalContentAlignment="Center"
                                       FontWeight="SemiBold"
                                       Content="{TemplateBinding Tag}" />
                                <ScrollViewer x:Name="PART_ContentHost"
                                              Grid.Column="1"
                                              IsTabStop="True"
                                              Margin="11 0"
                                              Background="{x:Null}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              TextElement.Foreground="{TemplateBinding Foreground}" />

                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Setter Property="VerticalContentAlignment"
                        Value="Center" />

                <Setter Property="HorizontalContentAlignment"
                        Value="Center" />

                <Setter Property="Cursor"
                        Value="IBeam" />

                <Setter Property="Height"
                        Value="40" />

                <Setter Property="Margin"
                        Value="0 5 0 5" />
            </Style.Setters>
        </Style>
    </UserControl.Resources>
    <Grid>
        <StackPanel Orientation="Vertical">
            <TextBlock Text="EBox Main Power Status"
                       FontSize="25"
                       FontWeight="SemiBold"
                       Foreground="#273c75"
                       HorizontalAlignment="Center"
                       Margin="0 10" />
            <TextBox Tag="Energy Received"
                     Text="{Binding EnergyReceived, Mode=OneWay, StringFormat={}{0:F0} kWh}"
                     IsReadOnly="True" />
            <TextBox Tag="Max Active Power Total"
                     Text="{Binding MaxActivePowerTotal, Mode=OneWay,StringFormat={}{0:F3} kW}"
                     IsReadOnly="True" />
            <TextBox Tag="AB Voltage"
                     Text="{Binding ABVoltage, Mode=OneWay,StringFormat={}{0:F3} V}"
                     IsReadOnly="True" />
            <TextBox Tag="BC Voltage"
                     Text="{Binding BCVoltage, Mode=OneWay,StringFormat={}{0:F3} V}"
                     IsReadOnly="True" />
            <TextBox Tag="A Current"
                     Text="{Binding ACurrent, Mode=OneWay,StringFormat={}{0:F3} A}"
                     IsReadOnly="True" />
            <TextBox Tag="B Current"
                     Text="{Binding BCurrent, Mode=OneWay,StringFormat={}{0:F3} A}"
                     IsReadOnly="True" />
        </StackPanel>
    </Grid>
</UserControl>