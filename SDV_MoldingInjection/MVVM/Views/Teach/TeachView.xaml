<UserControl x:Class="SDV_MoldingInjection.MVVM.Views.TeachView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SDV_MoldingInjection.MVVM.Views"
             mc:Ignorable="d" 
             xmlns:uiControls="clr-namespace:EQX.UI.Controls;assembly=EQX.UI"
             xmlns:vm="clr-namespace:SDV_MoldingInjection.MVVM.ViewModels"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <ItemsControl ItemsSource="{Binding ProcessList}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <RadioButton Content="{Binding Name}"
                             FontSize="20"
                             Command="{Binding DataContext.TeachUnitSelectCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                             CommandParameter="{Binding Name}"
                             GroupName="TEACH_UNIT_RADIOBUTTON"
                             Style="{StaticResource ButtonLikeRadioButtonStyle}">
                        <RadioButton.IsChecked>
                            <MultiBinding Converter="{StaticResource ObjectSelectedConverter}"
                                      Mode="OneWay">
                                <Binding Path="DataContext.TeachPositionUnitVM.CurrentProcess"
                                     RelativeSource="{RelativeSource AncestorType=UserControl}" />
                                <Binding />

                            </MultiBinding>
                        </RadioButton.IsChecked>
                    </RadioButton>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="1" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
        </ItemsControl>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>
            <ContentControl Content="{Binding TeachPositionUnitVM}"
                        Grid.Column="1" />
        </Grid>
    </Grid>
</UserControl>
