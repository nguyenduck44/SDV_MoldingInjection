<UserControl x:Class="SDV_MoldingInjection.Controls.CarrierJigStatusView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:conv="clr-namespace:SDV_MoldingInjection.Converters"
             mc:Ignorable="d" d:DesignWidth="120">

    <UserControl.Resources>
        <Style x:Key="TightCardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="2"/>
            <Setter Property="Margin" Value="0,1"/>
            <Setter Property="Padding" Value="2"/>
        </Style>
    </UserControl.Resources>

    <Grid Background="White">
        <StackPanel Margin="2">
            <TextBlock Text="{Binding Name}" FontWeight="Bold" Foreground="#1A237E" FontSize="12" HorizontalAlignment="Center"/>
            <TextBlock Text="{Binding ResinWeight, StringFormat='W: {0}'}" Foreground="#78909C" FontSize="12" HorizontalAlignment="Center" Margin="0,0,0,2"/>

            <Button Height="50"
                    Content="{Binding HeadSkip, Converter={StaticResource HeadSkipStatusTextConverter}}"
                    Background="{Binding HeadSkip, Converter={StaticResource HeadSkipStatusBrushConverter}}"
                    Foreground="White" FontSize="14" FontWeight="Bold">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="2"/>
                    </Style>
                </Button.Resources>
            </Button>

            <!--<Border Style="{StaticResource TightCardStyle}">
                <StackPanel>
                    <TextBlock Text="DELAY" FontSize="9" Foreground="#9E9E9E" HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding DelayTime, StringFormat={}{0:0.00}s}" FontSize="13" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,-2,0,0"/>
                </StackPanel>
            </Border>-->

            <Border Style="{StaticResource TightCardStyle}" BorderBrush="#1976D2">
                <StackPanel>
                    <TextBlock Text="INJECT TIME" FontSize="9" Foreground="#1976D2" HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding InjectTime, StringFormat={}{0:0.00}s}" FontSize="13" FontWeight="Bold" Foreground="#1976D2" HorizontalAlignment="Center" Margin="0,-2,0,0"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource TightCardStyle}" BorderBrush="#9E9E9E">
                <StackPanel>
                    <TextBlock Text="VENT TIME" FontSize="9" Foreground="#9E9E9E" HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding VentTime, StringFormat={}{0:0.00}s}" FontSize="13" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,-2,0,0"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource TightCardStyle}" BorderBrush="#9E9E9E">
                <StackPanel>
                    <TextBlock Text="VEL-P" FontSize="9" Foreground="#9E9E9E" FontWeight="Bold" HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding PAxisInjectVelocity, StringFormat={}{0:0.00}}" FontSize="13" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,-2,0,0"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>