<UserControl x:Class="SDV_MoldingInjection.Controls.CDAStatusUnitView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SDV_MoldingInjection.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="50" Width="50">
    <UserControl.Resources>
        <Style TargetType="{x:Type GroupBox}">
            <Setter Property="Margin"
                    Value="0" />
            <Setter Property="Padding"
                    Value="0" />
            <Setter Property="BorderThickness"
                    Value="1" />
            <Setter Property="BorderBrush"
                    Value="#DDD" />
            <Setter Property="Background"
                    Value="White" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="GroupBox">
                        <Border CornerRadius="5"
                                BorderBrush="#DDD"
                                BorderThickness="1"
                                Background="White"
                                SnapsToDevicePixels="True">
                            <DockPanel>
                                <Border Background="#E1E1E2"
                                        DockPanel.Dock="Top"
                                        Padding="0"
                                        CornerRadius="5,5,0,0">
                                    <TextBlock Text="{TemplateBinding Header}"
                                               FontWeight="Bold"
                                               Foreground="Black"
                                               VerticalAlignment="Center"
                                               HorizontalAlignment="Center"
                                               FontSize="9" />
                                </Border>

                                <ContentPresenter />
                            </DockPanel>
                        </Border>

                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid>
        <GroupBox Header="{Binding CDAName}"
                  FontSize="10">
            <TextBox IsReadOnly="True"
                     Background="{Binding IsThreshold, Converter={StaticResource BooleanToOnOffBrushConverter}}"
                     FontWeight="Bold"
                     FontSize="9"
                     Foreground="Black"
                     TextAlignment="Center"
                     VerticalContentAlignment="Center"
                     Text="{Binding CDAValue, StringFormat='{}{0:0.###} MPa'}" />
        </GroupBox>
    </Grid>
</UserControl>
